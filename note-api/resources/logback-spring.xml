<?xml version="1.0" encoding="UTF-8"?>
<!-- 属性描述 scan：性设置为true时，配置文件如果发生改变，将会被重新加载，默认值为true scanPeriod:设置监测配置文件是否有修改的时间间隔，
    如果没有给出时间单位，默认单位是毫秒。当scan为true时，此属性生效。默认的时间间隔为1分钟。
    debug:当此属性设置为true时，将打印出logback内部日志信息，实时查看logback运行状态。默认值为false。 -->
<configuration scan="true" scanPeriod="60 seconds" debug="false">

    <!-- ROOT日志级别-->
    <springProperty scope="context" name="ROOT_LEVEL" source="logging.level.root"/>

    <!-- 定义日志文件 输出位置 -->
    <property name="LOG_DIR" value="${user.home}/logs"/>
    <!-- 日志文件保留天数 -->
    <property name="MAX_HISTORY" value="3"/>
    <!-- 日志文件大小上限 -->
    <property name="MAX_FILE_SIZE" value="1GB"/>
    <!-- 日志开始下标 -->
    <property name="MIN_INDEX" value="0"/>
    <!-- 日志结束下标 -->
    <property name="MAX_INDEX" value="3"/>
    <!--
    日志输出格式:
        %d: 日期时间
        %-5level: 级别从左显示5个字符宽度,第一可选修饰符是左对齐标志,符号是减号"-"
        %thread: 线程名
        %logger{50}: 表示logger名字最长50个字符,否则按照句点分割
        %%line: 行号
        %msg: 日志消息
        %n: 是换行符
        %-15.15(): 如果记录的线程字符长度小于15(第一个)则用空格在右侧补齐,如果字符长度大于15(第二个),则从开头开始截断多余的字符
        %-50.50(): 如果记录的logger字符长度小于50(第一个)则用空格在右侧补齐,如果字符长度大于50(第二个),则从开头开始截断多余的字符
        %highlight(): 颜色,info为蓝色,warn为浅红,error为加粗红,debug为黑色
        %boldMagenta: 粗红
        %magenta: 洋红
        $cyan: 青色
        %white: 白色
    -->
    <property name="LOG_PATTERN" value="%d{MM-dd HH:mm:ss.SSS} [%X{tid}] %highlight(%-5level) %magenta([%20.20(%thread)]) ${HOSTNAME} %cyan(%50.50(%logger{50})):%-5.5(%line) - %msg%n"/>

    <!-- ConsoleAppender 控制台输出日志 -->
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <!--带颜色输出-->
            <pattern>${LOG_PATTERN}</pattern>
        </encoder>
    </appender>
    <!-- 所有级别日志 appender -->
    <appender name="ALL" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_DIR}/all.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.FixedWindowRollingPolicy">
            <fileNamePattern>${LOG_DIR}/_all-log-%i.log</fileNamePattern>
            <minIndex>${MIN_INDEX}</minIndex>
            <maxIndex>${MAX_INDEX}</maxIndex>
        </rollingPolicy>
        <triggeringPolicy class="ch.qos.logback.core.rolling.SizeBasedTriggeringPolicy">
            <maxFileSize>${MAX_FILE_SIZE}</maxFileSize>
        </triggeringPolicy>
        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
        </encoder>
    </appender>
    <appender name="ASYNC_ALL" class="ch.qos.logback.classic.AsyncAppender">
        <discardingThreshold >0</discardingThreshold>
        <includeCallerData>true</includeCallerData>
        <queueSize>1000</queueSize>
        <appender-ref ref="ALL"/>
    </appender>

    <!--
    <logger> 用来设置某一个包或者具体的某一个类的日志打印级别,以及指定appender
        name: 用来指定受此logger约束的某一个包或者具体的某一个类
        level: 用来设置打印级别
        additivity: 是否向上级logger传递打印信息,默认是true
    -->
    <logger name="springfox.documentation" level="WARN" />
    <logger name="org.springframework.security.access" level="INFO" />
    <logger name="org.apache.dubbo" level="WARN" />
    <logger name="com.alibaba.nacos" level="WARN" />
    <logger name="com.alibaba.cloud.nacos" level="WARN" />
    <logger name="org.springframework" level="INFO" />
    <logger name="org.soul.data" level="${ROOT_LEVEL}" />
    <logger name="org.soul.ms" level="${ROOT_LEVEL}"/>


    <!-- root级别 -->
    <root level="${ROOT_LEVEL}">
        <appender-ref ref="ASYNC_ALL"/>
        <!-- 只输出至控制台 -->
        <appender-ref ref="STDOUT" />
    </root>
</configuration>
